Feature: To Add Service
    As an admin, I can add services, so that user could book the services whenever they need

    Scenario: Admin provides service name, service price, service desc, service category and  press submit button to add product in the application
        Given Test Service Add functionality
